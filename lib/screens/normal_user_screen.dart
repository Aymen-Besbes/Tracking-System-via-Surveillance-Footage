
import 'package:flutter/material.dart';

class NormalUser extends StatelessWidget {
  const NormalUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold() ;const Placeholder();
  }
}

